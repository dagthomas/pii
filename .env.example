# Point the nordic-v9 engine at a remote model (e.g. Hugging Face).
# The .data file must sit next to it: <url>.data is fetched automatically.
# VITE_MODEL_URL=https://huggingface.co/<user>/nordic-redactor-onnx/resolve/main/nordic-v9-web.onnx
